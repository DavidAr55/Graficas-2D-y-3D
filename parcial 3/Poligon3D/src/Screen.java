import javax.swing.*;

public class Screen extends JFrame {

    public Screen() {
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setSize(500, 500);
        setLocationRelativeTo(null);
        setVisible(true);
        setTitle("Poligono 3D");
    }
}
